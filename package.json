{
  "name": "signalx",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "zip": "npm run build && rm -f signalx-v1.0.0.zip && cd dist && zip -r ../signalx-v1.0.0.zip . -x '.*'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.1.0",
    "zod": "^3.23.0"
  }
}
